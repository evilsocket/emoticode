require 'caronte'

User.all.each do |u|
    unless u.last_login_ip.nil?
      check = Caronte.check u.last_login_ip
      unless check.values.empty?
        puts "#{u.username} (#{u.email}) : #{check.inspect}"
      end
    end
  end
