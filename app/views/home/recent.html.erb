<section id="home">
  <ul class="listing">
    <%= render :partial => "source/listing", :collection => @recent, :as => 'source' %>
  </ul>
  <%= render :partial => 'layouts/sidebar', :locals => { with_heading: false } %>
</section>

<br style="clear:both"/>

<section class="pager">
  <%= will_paginate @recent %>
</section>

